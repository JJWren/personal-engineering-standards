# .NET General Coding Standards

- Use C# 12+ features where appropriate.
- Follow PascalCase for public members, camelCase for private members.
- Use File-Scoped Namespaces.
- Prefer `primary constructors` for simple classes.
- Always use `Task` for asynchronous operations.
- Ensure all public methods have XML documentation.
